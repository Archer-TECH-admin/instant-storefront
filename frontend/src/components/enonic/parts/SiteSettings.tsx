// This part holds no visual output — it is a configuration container.
// Editors drop it onto the Home page in Content Studio to configure the
// storefront-wide settings (store name, nav links, footer, etc.).
// fetchSiteSettings() reads its config from pageAsJson in draft mode.
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export default function SiteSettings(_: any) {
  return null
}
